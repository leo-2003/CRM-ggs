// INSTRUCTIONS:
// 1. Create a copy of this file and name it `config.ts`.
// 2. Fill in your actual Supabase credentials in `config.ts`.
// 3. Make sure to add `config.ts` to your `.gitignore` file to keep your keys secure.

export const SUPABASE_URL = 'YOUR_SUPABASE_URL'; // Replace with your Supabase project URL
export const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY'; // Replace with your Supabase anon key